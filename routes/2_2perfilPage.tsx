import { Usuario } from "../components/Usuario.tsx";
import { perfilPropio } from "/routes/[name].tsx";






const Page = (props: PageProps<unknown>) => {
    return (
        <AddManager/>
    )
};

export default Page;

